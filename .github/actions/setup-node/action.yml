name: Setup
description: "それぞれのJob前のセットアップ"

runs:
    using: composite
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.12.0'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci
        shell: bash